import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles((theme) => ({
  table: {
    minWidth: 650,
  },
  summary: {
    fontWeight: 'bold',
  },
  discount: {
    color: 'green',
    fontWeight: 'bold',
  },
}))